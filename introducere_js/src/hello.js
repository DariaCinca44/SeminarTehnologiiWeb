let sayHello= (name) => {
    return `Hello, ${name}!`;
};

console.log(sayHello(process.argv[2]));

let concatenare= (arr) => {
    return arr.join('');
};

cuv = ["introducere", " ", "js"];

console.log(concatenare(cuv));